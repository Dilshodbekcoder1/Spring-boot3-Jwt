server:
  port: 8086
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project_jwt
    username: Jwt
    password: dilshod
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


